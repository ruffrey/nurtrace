package potential

/*
Train runs a series training sessions once for all the available data.

We should assume that the perceptors and receptors are present.
*/
func Train(network *Network) (err error) {
	network.ResetForTraining()

	return nil
}
